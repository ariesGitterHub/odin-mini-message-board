<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/nav.ejs') %>
    <div class="main-board">
      <h2 class="page-title">Main Board - Latest Messages</h2>
      <%# if (messages.length > 0) { %> <%# messages.forEach(item => { %>
      <% if (messages) { %> <% messages.forEach(item => { %>
      <div class="message-cards">
        <!-- <h3 class="user-name"><%#= item.user %></h3> -->
        <h3 class="author-name"><%= item.author %></h3>
        <p class="added-when"><%= item.added %></p>
        <!-- <p class="id-order">Msg#: <%#= item.id + 1 %></p> -->
        <hr />
        <p class="text-message"><%= item.text %></p>
        <div class="bottom-card-items">
          <a href="/new">New Posting</a>
          <form method="POST" action="/like/<%= item.id %>">
            <!-- <button>
              <% if (item.likes > 0) { %>
              <p>💙</p>
              <% } else { %>
              <p>🤍</p>
              <% } %> &nbsp;
              <p class="likes-number"><%= item.likes %></p>
            </button> -->
            <!-- Below is better code... -->
             <button>
              <span><%= item.likes > 0 ? "💙" : "🤍" %></span>
              &nbsp;
              <span class="likes-number"><%= item.likes %></span>
            </button>
          </form>
        </div>
      </div>
      <% }) %> <% } else { %>
      <p class="no-message-to-show">
        Currently, there are no messages to display.
      </p>
      <% } %>
    </div>
    <%- include('./partials/footer.ejs') %>
  </body>
</html>
